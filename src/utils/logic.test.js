const {randomizePlayerStart} = require('./logic')

describe('randomizes which player plays light or dark', () => {
    test('challenged is truthy', () => {})
    test('challenger is truthy', () => {})
    test('randoCalrizian is correctly assigned a value between 0 and 1', {})
    test('user one is assigned as dark if value is less than .6', () => {})
    test('user one is assigned as light if value is greater than .6', () => {})
    test('the second user passed in gets assigned dark if user one is light', () => {})
    test('the second user passed in gets assigned light if user one is dark', () => {})
})