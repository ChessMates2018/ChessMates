// const {randomizePlayerStart} = require('./logic')

// describe('randomizes which player plays light or dark', () => {
//     test('challenged, and challenger are truthy', () => {
//         expect(randomizePlayerStart('harry', 'bob')).toBeTruthy()
//     })
//     test('if one of the values passed in is undefined throw error', () => {
//         expect(randomizePlayerStart(undefined, undefined).lightPlayer).toBeFalsy()
//     })
//     test('randoCalrizian is correctly assigned a value between 0 and 1', () => {
//         expect(randomizePlayerStart('harry', 'bob'). 
//         randoCalrizian > 0 && randomizePlayerStart('harry', 'bob').randoCalrizian < 1).toBe(true)
//     })
//     test('when randoCalrizian is > .06 light player === challenger', () => {
//         expect(randomizePlayerStart('harry', 'bob').lightPlayer).toEqual('harry')
//     })
//     test('when randoCalrizian is > .06 dark player === challenged', () => {
//         expect(randomizePlayerStart('harry', 'bob').darkPlayer).toEqual('bob')
//     })

    // test('challenged gets assigned dark if challenger is light', () => {
    //     expect(randomizePlayerStart('harry', 'bob').lightPlayer === 'harry' && randomizePlayerStart('harry', 'bob').darkPlayer === 'bob'
    //     )}).toBe(true)


        
//     // test('challenged gets assigned light if challenger is dark', () => {
//     //     expect(light === challenged && dark === challenger)
//     // }).toBe(true)
   
    // test('challenger is assigned as dark if value is less than .6', () => {
    //     expect(randomizePlayerStart('harry', 'bob')
    // )}).toEql(randoCalrizian < .6 ? dark === challenger: light === challenger)



// //     test('challenger is assigned as light if value is greater than .6', () => {})
})